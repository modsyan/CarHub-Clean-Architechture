namespace Mac.CarHub.Domain.Constants;

public abstract class Roles
{
    public const string Administrator = nameof(Administrator);
    public const string Broker = nameof(Broker);
    public const string User = nameof(User);
}
